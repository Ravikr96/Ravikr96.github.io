# Legacy iPortfolio template

This folder contains the previous Bootstrap-based portfolio (iPortfolio template).

The new site is built with Vite + React + TypeScript in the repo root. Images were migrated to `public/`.

Safe to delete this folder once you confirm the new site works.
